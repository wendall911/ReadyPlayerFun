{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${version}",
  "name": "${mod_name}",
  "description": "${mod_description}",
  "authors": [
    "${mod_author}"
  ],
  "contact": {
    "issues": "${issue_tracker}"
  },
  "license": "${license}",
  "icon": "${mod_id}_icon.png",
  "environment": "*",
  "entrypoints": {
    "main": [
      "readyplayerfun.ReadyPlayerFunFabric"
    ],
    "spectrelib": [
      "readyplayerfun.FabricConfigInitializer"
    ]
  },
  "mixins": [
    "readyplayerfun.fabric.mixins.json"
  ],
  "depends": {
    "fabricloader": "${fabric_loader_min_version}",
    "fabric": "*",
    "minecraft": "${fabric_mc_version_range}",
    "java": ">=17"
  }
}
